export const mailTo = (e:  React.MouseEvent<HTMLElement>) => {
    e.preventDefault();
    window.location.href = 'mailto:chubi.best@gmail.com'
}

export const about = (e: React.MouseEvent<HTMLElement>) => {
    e.preventDefault();
}